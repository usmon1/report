# report````markdown
# 🚀 Enterprise Reporting System

Платформа для управления и анализа данных предприятий и предоставляемых ими услуг.  
Проект включает веб-интерфейс и систему аутентификации, интегрированную с двумя базами данных — **основной (предприятия, услуги, отчёты)** и **пользовательской (аутентификация и роли)**.

---

## 📦 Стек технологий
- **Backend:** Python, FastAPI  
- **Frontend:** HTML, CSS, шаблоны Jinja2  
- **База данных:** PostgreSQL (две БД — Prod и Auth)  
- **ORM:** psycopg2 (прямое подключение)  
- **Контейнеризация:** Docker, Docker Compose  
- **Безопасность:** bcrypt, контроль неудачных попыток входа  

---

## 🚀 Быстрый запуск проекта

```bash
git clone https://github.com/usmon1/report.git
cd report/Practise_make_perfect
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
````

После запуска приложение будет доступно по адресу:

> 🔗 [http://127.0.0.1:8000](http://127.0.0.1:8000)
> или
> 🔗 [http://0.0.0.0:8000](http://0.0.0.0:8000)

---

## ⚙️ Пошаговое объяснение команд

### 1. **Клонирование репозитория**

```bash
git clone https://github.com/usmon1/report.git
```

Скачивает проект с GitHub на локальный компьютер.

### 2. **Переход в директорию проекта**

```bash
cd report/Practise_make_perfect
```

Переход в рабочую директорию, где находится код приложения.

### 3. **Создание виртуального окружения**

```bash
python3 -m venv .venv
```

Создаёт изолированную среду Python для установки зависимостей проекта.

### 4. **Активация виртуального окружения**

```bash
source .venv/bin/activate
```

Активирует среду `.venv`, чтобы `pip` устанавливал библиотеки внутрь проекта.

> 💡 В Windows используйте:
>
> ```bash
> .venv\Scripts\activate
> ```

### 5. **Установка зависимостей**

```bash
pip install -r requirements.txt
```

Устанавливает все библиотеки, необходимые для работы проекта (FastAPI, Uvicorn, bcrypt и т.д.).

### 6. **Запуск приложения**

```bash
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

* `uvicorn` — сервер для FastAPI
* `app.main:app` — путь к объекту FastAPI-приложения
* `--reload` — автоперезапуск при изменении кода
* `--host 0.0.0.0` — делает сервер доступным в сети
* `--port 8000` — порт для запуска приложения

---

## 🧩 Структура проекта

```
Practise_make_perfect/
├── app/
│   ├── main.py                  # Основное приложение FastAPI
│   ├── database.py              # Подключение к БД
│   ├── auth/                    # Модуль аутентификации
│   │   ├── routes.py            # Проверка логина, регистрация, обновление входа
│   │   ├── security.py          # Защита от брутфорса, блокировка после 5 попыток
│   │   └── utils.py             # Хэширование и проверка пароля (bcrypt)
│   └── templates/               # HTML-шаблоны
│       ├── index.html
│       ├── auth/ (login, register, check_auth)
│       ├── user/ (dashboard, reports_main, catalogs)
│       ├── admin/ (dashboard, enterprises, periods, services, users)
│       └── reports/ (enterprise, district)
├── requirements.txt
└── scripts.sql
```

---

## 🔐 Аутентификация и безопасность

### Основные функции (`app/auth/routes.py`)

* `check_user_login(login, password)` — проверяет логин и пароль, верифицирует bcrypt-хэш, обновляет время последнего входа.
* `check_login_unique(login)` — проверяет, свободен ли логин.
* `check_email_unique(email)` — проверяет уникальность email.
* `create_user(full_name, email, login, password)` — создаёт нового пользователя с хэшированным паролем.

### Механизм безопасности (`app/auth/security.py`)

* До 5 неудачных попыток входа → блокировка на 15 минут.
* Сброс счётчика после успешного входа.
* Отслеживание комбинации логин + IP.

### Хэширование паролей (`app/auth/utils.py`)

* Используется **bcrypt**.
* Хэширование пароля при регистрации и проверка при входе.

---

## 🧭 Структура интерфейса

### 1. 🏠 Главная страница

Отображает сводные данные:

* Количество предприятий
* План и факт выполнения услуг
* Процент выполнения плана

---

### 2. 📊 Раздел «Отчёты»

* **Отчёт по предприятию** — детализированный анализ по услугам, районам и периодам.
* **Сводный отчёт по району** — агрегированные показатели по всем предприятиям выбранного района.
* Экспорт в **PDF**.

---

### 3. 📚 Раздел «Справочники»

Справочная информация:

* Предприятия
* Министерства
* Отрасли
* Области и районы
* Виды услуг

---

### 4. 🛠️ Интерфейс администратора

Разделы панели администратора:

* **🏢 Предприятия** — управление таблицей `enterprises`
* **⚙️ Услуги** — управление таблицей `services`
* **📅 Отчётные периоды** — управление таблицей `period`
* **👤 Пользователи** — управление записями `users`

Каждый раздел включает:

* Таблицу с фильтрацией и поиском
* Кнопки: **Добавить / Редактировать / Удалить**
* Модальные окна с интегрированными справочниками

---

## 🗃️ Структура базы данных

### Основная база (enterprise_db)

* `service_types` — виды услуг
* `ministries` — министерства
* `industries` — отрасли
* `regions` — области
* `districts` — районы (FK → regions)
* `enterprises` — предприятия (FK → ministries, industries, regions)
* `period` — отчётные периоды (FK → enterprises)
* `services` — услуги (FK → enterprises, districts, period, service_types)

### База пользователей (users_db)

* `users` — пользователи системы (ФИО, логин, пароль_хэш, роль, статус, даты)

---

## 🧠 Основные возможности

* Аутентификация пользователей (роль **user/admin**)
* Защита от брутфорса (до 5 попыток входа)
* Панель администратора с CRUD-функциями
* Генерация отчётов по предприятиям и районам
* Экспорт отчётов в PDF

---

## 📄 Лицензия

**MIT License**

---


```
```
